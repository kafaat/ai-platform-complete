apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
  namespace: ai-platform
data:
  LOG_LEVEL: "info"
  NODE_ENV: "production"
  REDIS_HOST: "redis-service"
  DB_HOST: "postgres-service"
